services:
 
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: LetMe1n
    volumes: 
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
